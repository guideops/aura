[Service]
# Both Claude Code accounts on this machine. Watching only ~/.claude silently
# hides every session run under the other account.
Environment="AURA_TRANSCRIPTS=/root/.claude/projects,/root/.claude-acct2/projects"
